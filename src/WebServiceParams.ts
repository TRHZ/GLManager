const host = '192.168.1.84'; // Cambia esto por la dirección IP de tu máquina
const port = 3050; // Asegúrate de que este puerto coincida con el puerto en el que se está ejecutando tu servicio web

export default {host, port};
